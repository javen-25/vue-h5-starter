# Staging 环境基础路径（如有子路径部署可改为 '/h5/'）
VITE_BASE=/

# Staging 接口基础路径（可改为完整后端地址，如 https://staging.example.com/api）
VITE_API_BASE_URL=/api

# 是否启用 MSW（Mock Service Worker）。Staging 默认关闭。
VITE_ENABLE_MSW=false

